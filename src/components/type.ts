
    type TMapContent = {
        key : string,
        container: HTMLElement | string,
        zoom: number,
        latitude: number,
        longitude: number,
    }

    export type {TMapContent}